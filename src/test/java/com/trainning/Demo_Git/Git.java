package com.trainning.Demo_Git;

public class Git {
/*
	commands :-
	-git init
	-git status
	-git remote add origin <Repo URL> (Copy URL from git Websit)
	-git remote -v(To view the connection)
	
	create .gitignore file
	-git commit -m "Message"
	-git push origin master
	-this is branch Master now this will not save in branch A
	*/
}
